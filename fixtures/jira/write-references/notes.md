---
section: "Links (write direction) — reference style"
direction: write
---
Reference links and images are resolved against their definitions before writing; the definitions themselves emit nothing, so the readback is the inline form.
