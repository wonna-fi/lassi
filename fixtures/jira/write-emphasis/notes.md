---
section: "Composite (write direction) — emphasis"
direction: write
---
Nested emphasis, code and links inside emphasis, Jira's `{*}` intra-word form when a neighbour is a word character or a non-opener, inner closer-position markers escaped, and a hard break inside emphasis written as `\\\\` so the run stays on one line.
