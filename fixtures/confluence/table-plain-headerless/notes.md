---
section: "Confluence Storage Format — Tables (no header)"
direction: both
roundtrip: true
writer: {tableShell: plain, cellWrap: none}
---
GFM needs a header row, so a header-less storage table reads with an empty header row and writes back without `th`.
