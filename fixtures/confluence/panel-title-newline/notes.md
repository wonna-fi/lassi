---
section: "Confluence Storage Format — Info, Tip, Note and Warning macros"
direction: read
roundtrip: false
---
A panel title goes on the `[!KIND] …` marker line, which is one line by construction. A title
carrying a line ending used to be copied onto it anyway, and the second line came back as body text
on the next read, so the panel is fenced instead.
