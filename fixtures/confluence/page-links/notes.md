---
section: "Confluence Storage Format — Links to pages"
direction: both
roundtrip: true
---
A bare title that itself looks like `SPACE:Title` is written `[[:Release:notes]]` so it is not read as a space key.
