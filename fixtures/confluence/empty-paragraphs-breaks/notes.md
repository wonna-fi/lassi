---
section: "Confluence Storage Format — Line breaks and empty paragraphs"
direction: both
roundtrip: true
---
An empty paragraph (`<p><br/></p>`) is a flow `<br />` in markdown; a break inside a paragraph is a hard break.
